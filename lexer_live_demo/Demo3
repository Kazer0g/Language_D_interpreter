// Demo 3 — strings: no escape sequences, backslash is a literal character
print "no escapes here \n stays as backslash-n"   // \n is NOT decoded —
                                                     // just two literal chars

print 'single quotes work too'

var path := "C:\some\path"   // backslash is just a regular character,
                              // no special meaning at all
print path