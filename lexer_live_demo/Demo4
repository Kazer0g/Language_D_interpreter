// Demo 4 — identifiers: case-sensitive, [A-Za-z_][A-Za-z0-9_]*
var myVar1 := 1
var _x := 1
var A := 1
print A          // 1 — same identifier

var a := 2
print A           // 1 — "a" and "A" = 1 are two DIFFERENT identifiers